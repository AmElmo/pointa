{
  "name": "pointa",
  "version": "1.3.6",
  "type": "module",
  "private": true,
  "description": "Pointa - AI-powered development annotations",
  "scripts": {
    "firefox:build": "node scripts/build-firefox-extension.js",
    "firefox:lint": "npm run firefox:build && npx --yes web-ext lint --source-dir dist/firefox",
    "firefox:run": "npm run firefox:build && npx --yes web-ext run --source-dir dist/firefox",
    "firefox:package": "npm run firefox:build && npx --yes web-ext build --source-dir dist/firefox --artifacts-dir dist/firefox-artifacts --overwrite-dest",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  },
  "dependencies": {
    "pointa-server": "^1.0.0"
  }
}
